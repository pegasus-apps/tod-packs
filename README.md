# tod-packs

Internal static asset repository. Contains structured resource bundles
used by the app at runtime. Files are versioned and consumed programmatically.

Not intended for manual use outside of the application context.